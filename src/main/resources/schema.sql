/*create TABLE Course(
    id bigint not null ,
    name varchar(255) not null ,
    author varchar(225) not null,
    primary key (id)
);
create table UserDetails(
    id bigint not null ,
    name varchar(255) not null ,
    birthDate date not null ,
    primary key (id)
);

insert into User_Details (BIRTH_DATE,ID,NAME,PASSWORD) VALUES (CURRENT_DATE,1,'Sharanya','Password');

insert into Post (ID, USER_ID, DESCRIPTION) VALUES (101,1,'I want to Read Spring Boot');

 */